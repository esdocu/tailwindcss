import { ApiTable } from "@/components/api-table.tsx";
import { Example } from "@/components/example.tsx";
import { Figure } from "@/components/figure.tsx";
import { ResponsiveDesign } from "@/components/content.tsx";

export const title = "text-align";
export const description = "Utilidades para controlar la alineación del texto.";

<ApiTable
  rows={[
    ["text-left", "text-align: left;"],
    ["text-center", "text-align: center;"],
    ["text-right", "text-align: right;"],
    ["text-justify", "text-align: justify;"],
    ["text-start", "text-align: start;"],
    ["text-end", "text-align: end;"],
  ]}
/>

## Ejemplos

### Alinear texto a la izquierda

Usa la utilidad `text-left` para alinear el texto de un elemento a la izquierda:

<Figure>

<Example padding={false}>
  {
    <div className="px-4 sm:px-0">
      <div className="mx-auto max-w-lg p-8 text-sm/6 text-gray-900 dark:text-gray-200">
        <p className="text-left">
          Así que empecé a adentrarme en el agua. No les voy a mentir muchachos, estaba aterrorizado. Pero seguí adelante, y a medida que avanzaba más allá de las olas, una extraña calma se apoderó de mí. No sé si fue una intervención divina o el parentesco de todos los seres vivos, pero te digo Jerry, en ese momento, yo <em>era</em> un biólogo marino.
        </p>
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:text-left] -->
<p class="text-left">Así que empecé a adentrarme en el agua...</p>
```

</Figure>

### Alinear texto a la derecha

Usa la utilidad `text-right` para alinear el texto de un elemento a la derecha:

<Figure>

<Example padding={false}>
  {
    <div className="px-4 sm:px-0">
      <div className="mx-auto max-w-lg p-8 text-sm/6 text-gray-900 dark:text-gray-200">
        <p className="text-right">
          Así que empecé a adentrarme en el agua. No les voy a mentir muchachos, estaba aterrorizado. Pero seguí adelante, y a medida que avanzaba más allá de las olas, una extraña calma se apoderó de mí. No sé si fue una intervención divina o el parentesco de todos los seres vivos, pero te digo Jerry, en ese momento, yo <em>era</em> un biólogo marino.
        </p>
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:text-right] -->
<p class="text-right">Así que empecé a adentrarme en el agua...</p>
```

</Figure>

### Centrar texto

Usa la utilidad `text-center` para centrar el texto de un elemento:

<Figure>

<Example padding={false}>
  {
    <div className="px-4 sm:px-0">
      <div className="mx-auto max-w-lg p-8 text-sm/6 text-gray-900 dark:text-gray-200">
        <p className="text-center">
          Así que empecé a adentrarme en el agua. No les voy a mentir muchachos, estaba aterrorizado. Pero seguí adelante, y a medida que avanzaba más allá de las olas, una extraña calma se apoderó de mí. No sé si fue una intervención divina o el parentesco de todos los seres vivos, pero te digo Jerry, en ese momento, yo <em>era</em> un biólogo marino.
        </p>
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:text-center] -->
<p class="text-center">Así que empecé a adentrarme en el agua...</p>
```

</Figure>

### Justificar texto

Usa la utilidad `text-justify` para justificar el texto de un elemento:

<Figure>

<Example padding={false}>
  {
    <div className="px-4 sm:px-0">
      <div className="mx-auto max-w-lg p-8 text-sm/6 text-gray-900 dark:text-gray-200">
        <p className="text-justify">
          Así que empecé a adentrarme en el agua. No les voy a mentir muchachos, estaba aterrorizado. Pero seguí adelante, y a medida que avanzaba más allá de las olas, una extraña calma se apoderó de mí. No sé si fue una intervención divina o el parentesco de todos los seres vivos, pero te digo Jerry, en ese momento, yo <em>era</em> un biólogo marino.
        </p>
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:text-justify] -->
<p class="text-justify">Así que empecé a adentrarme en el agua...</p>
```

</Figure>

### Uso de propiedades lógicas

Usa las utilidades `text-start` y `text-end`, las cuales utilizan [propiedades lógicas](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Logical_Properties/Basic_concepts) para asignarse al lado izquierdo o derecho según la dirección del texto:

<Figure>

<Example padding={false}>
  {
    <div className="px-4 sm:px-0" dir="rtl" lang="ar">
      <div className="mx-auto max-w-lg p-8 text-sm/6 text-gray-900 dark:text-gray-200">
        <p className="text-end">
          بدأتُ أسير نحو الماء. لن أكذب عليكم يا رفاق، كنتُ مرعوبًا. لكنني واصلتُ المسير، وبينما كنتُ أشق طريقي عبر
          الأمواج، غمرني هدوءٌ غريب. لا أعلم إن كان ذلك تدخّلًا إلهيًا أم صلة قرابة بين جميع الكائنات الحية، لكنني أقول
          لك يا جيري، في تلك اللحظة، كنتُ عالم أحياء بحرية.
        </p>
      </div>
    </div>
  }
</Example>

```html
<!-- [!code word:dir="rtl"] -->
<!-- [!code classes:text-end] -->
<div dir="rtl" lang="ar">
  <p class="text-end">فبدأت بالسير نحو الماء...</p>
</div>
```

</Figure>

### Diseño responsivo

<ResponsiveDesign element="p" property="text-align" defaultClass="text-left" featuredClass="text-center" />
