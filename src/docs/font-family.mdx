import { ApiTable } from "@/components/api-table.tsx";
import { CustomizingYourTheme, ResponsiveDesign, UsingACustomValue } from "@/components/content.tsx";
import { Example } from "@/components/example.tsx";
import { Figure } from "@/components/figure.tsx";

export const title = "font-family";
export const description = "Utilidades para controlar la familia tipográfica (font-family) de un elemento.";

<ApiTable
  rows={[
    [
      "font-sans",
      "font-family: var(--font-sans); /* ui-sans-serif, system-ui, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol', 'Noto Color Emoji' */",
    ],
    ["font-serif", "font-family: var(--font-serif); /* ui-serif, Georgia, Cambria, 'Times New Roman', Times, serif */"],
    [
      "font-mono",
      "font-family: var(--font-mono); /* ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace */",
    ],
    ["font-(family-name:<custom-property>)", "font-family: var(<custom-property>);"],
    ["font-[<value>]", "font-family: <value>;"],
  ]}
/>

## Ejemplos

### Ejemplo básico

Utiliza utilidades como `font-sans` y `font-mono` para establecer la familia tipográfica de un elemento:

<Figure>

<Example>
  {
    <div className="flex flex-col gap-8">
      <div>
        <span className="mb-3 font-mono text-xs font-medium text-gray-500 dark:text-gray-400">font-sans</span>
        <p className="font-sans text-lg font-medium text-gray-900 dark:text-gray-200">
          El veloz zorro marrón salta sobre el perro perezoso.
        </p>
      </div>
      <div>
        <span className="mb-3 font-mono text-xs font-medium text-gray-500 dark:text-gray-400">font-serif</span>
        <p className="font-serif text-lg font-medium text-gray-900 dark:text-gray-200">
          El veloz zorro marrón salta sobre el perro perezoso.
        </p>
      </div>
      <div>
        <span className="mb-3 font-mono text-xs font-medium text-gray-500 dark:text-gray-400">font-mono</span>
        <p className="font-mono text-lg font-medium text-gray-900 dark:text-gray-200">
          El veloz zorro marrón salta sobre el perro perezoso.
        </p>
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:font-sans,font-serif,font-mono] -->
<p class="font-sans ...">El veloz zorro marrón ...</p>
<p class="font-serif ...">El veloz zorro marrón ...</p>
<p class="font-mono ...">El veloz zorro marrón ...</p>
```

</Figure>

### Usar un valor personalizado

<UsingACustomValue utility="font" name="familia tipográfica" value="Open_Sans" element="p" dataType="family-name" />

### Responsive design

<ResponsiveDesign property="font-family" defaultClass="font-sans" featuredClass="font-serif" element="p" />

## Personalizar tu tema

<CustomizingYourTheme
  utility="font"
  name="familia tipográfica"
  customName="display"
  customValue='"Oswald", sans-serif'
>

También puedes proporcionar valores predeterminados para `font-feature-settings` y `font-variation-settings` para una familia tipográfica:

```css
@theme {
  --font-display: "Oswald", sans-serif;
  --font-display--font-feature-settings: "cv02", "cv03", "cv04", "cv11"; /* [!code highlight] */
  --font-display--font-variation-settings: "opsz" 32; /* [!code highlight] */
}
```

Si es necesario, utiliza la regla `@font-face` para cargar fuentes personalizadas:

```css
@font-face {
  font-family: Oswald;
  font-style: normal;
  font-weight: 200 700;
  font-display: swap;
  src: url("/fonts/Oswald.woff2") format("woff2");
}
```

Si estás cargando una fuente desde un servicio como [Google Fonts](https://fonts.google.com/), asegúrate de colocar la regla `@import` en la parte superior de tu archivo CSS:

```css
@import url("https://fonts.googleapis.com/css2?family=Roboto&display=swap"); /* [!code highlight] */
@import "tailwindcss";

@theme {
  --font-roboto: "Roboto", sans-serif; /* [!code highlight] */
}
```

Los navegadores requieren que las declaraciones `@import` aparezcan antes que cualquier otra regla, por lo que las importaciones de URL deben estar por encima de importaciones como `@import "tailwindcss"`, las cuales se incluyen en línea en el CSS compilado.

</CustomizingYourTheme>
