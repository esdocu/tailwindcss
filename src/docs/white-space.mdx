import { ApiTable } from "@/components/api-table.tsx";
import { Example } from "@/components/example.tsx";
import { Figure } from "@/components/figure.tsx";
import { ResponsiveDesign } from "@/components/content.tsx";

export const title = "white-space";
export const description = "Utilidades para controlar la propiedad white-space de un elemento.";

<ApiTable
  rows={[
    ["whitespace-normal", "white-space: normal;"],
    ["whitespace-nowrap", "white-space: nowrap;"],
    ["whitespace-pre", "white-space: pre;"],
    ["whitespace-pre-line", "white-space: pre-line;"],
    ["whitespace-pre-wrap", "white-space: pre-wrap;"],
    ["whitespace-break-spaces", "white-space: break-spaces;"],
  ]}
/>

## Ejemplos

### Normal

Usa la utilidad `whitespace-normal` para hacer que el texto se ajuste normalmente dentro de un elemento. Los saltos de línea y los espacios se colapsarán:

<Figure>

<Example padding={false}>
  {
    <div className="mx-auto max-w-sm border-x border-x-pink-400/30 py-8 text-gray-900 dark:text-gray-200">
      <p
        className="whitespace-normal"
        children={
          "¡Hola a todos!\n\nCasi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.\n\nNunca lo sabrás."
        }
      />
    </div>
  }
</Example>

```html
<!-- [!code classes:whitespace-normal] -->
<!-- prettier-ignore -->
<p class="whitespace-normal">¡Hola a todos!

Casi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.

Nunca lo sabrás.</p>
```

</Figure>

### No Wrap (Sin ajuste de línea)

Usa la utilidad `whitespace-nowrap` para evitar que el texto se ajuste dentro de un elemento. Los saltos de línea y los espacios se colapsarán:

<Figure>

<Example padding={false}>
  {
    <div className="mx-auto max-w-sm border-x border-x-pink-400/30 py-8 text-gray-900 dark:text-gray-200">
      <p
        className="whitespace-nowrap"
        children={
          "¡Hola a todos!\n\nCasi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.\n\nNunca lo sabrás."
        }
      />
    </div>
  }
</Example>

```html
<!-- [!code classes:whitespace-nowrap] -->
<!-- prettier-ignore -->
<p class="overflow-auto whitespace-nowrap">¡Hola a todos!

Casi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.

Nunca lo sabrás.</p>
```

</Figure>

### Pre

Usa la utilidad `whitespace-pre` para conservar los saltos de línea y los espacios dentro de un elemento. El texto no se ajustará:

<Figure>

<Example padding={false}>
  {
    <div className="mx-auto max-w-sm border-x border-x-pink-400/30 py-8 text-gray-900 dark:text-gray-200">
      <p
        className="whitespace-pre"
        children={
          "¡Hola a todos!\n\nCasi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.\n\nNunca lo sabrás."
        }
      />
    </div>
  }
</Example>

```html
<!-- [!code classes:whitespace-pre] -->
<!-- prettier-ignore -->
<p class="overflow-auto whitespace-pre">¡Hola a todos!

Casi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.

Nunca lo sabrás.</p>
```

</Figure>

### Pre Line

Usa la utilidad `whitespace-pre-line` para conservar los saltos de línea pero no los espacios dentro de un elemento. El texto se ajustará normalmente:

<Figure>

<Example padding={false}>
  {
    <div className="mx-auto max-w-sm border-x border-x-pink-400/30 py-8 text-gray-900 dark:text-gray-200">
      <p
        className="whitespace-pre-line"
        children={
          "¡Hola a todos!\n\nCasi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.\n\nNunca lo sabrás."
        }
      />
    </div>
  }
</Example>

```html
<!-- [!code classes:whitespace-pre-line] -->
<!-- prettier-ignore -->
<p class="whitespace-pre-line">¡Hola a todos!

Casi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.

Nunca lo sabrás.</p>
```

</Figure>

### Pre Wrap

Usa la utilidad `whitespace-pre-wrap` para conservar los saltos de línea y los espacios dentro de un elemento. El texto se ajustará normalmente:

<Figure>

<Example padding={false}>
  {
    <div className="mx-auto max-w-sm border-x border-x-pink-400/30 py-8 text-gray-900 dark:text-gray-200">
      <p
        className="whitespace-pre-wrap"
        children={
          "¡Hola a todos!\n\nCasi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.\n\nNunca lo sabrás."
        }
      />
    </div>
  }
</Example>

{/* prettier-ignore */}
```html
<!-- [!code classes:whitespace-pre-wrap] -->
<!-- prettier-ignore -->
<p class="whitespace-pre-wrap">¡Hola a todos!

Casi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.

Nunca lo sabrás.</p>
```

</Figure>

### Break Spaces

Usa la utilidad `whitespace-break-spaces` para conservar los saltos de línea y los espacios dentro de un elemento. El espacio en blanco al final de las líneas no colgará, sino que se ajustará a la siguiente línea:

<Figure>

<Example padding={false}>
  {
    <div className="mx-auto max-w-sm border-x border-x-pink-400/30 py-8 text-gray-900 dark:text-gray-200">
      <p
        className="whitespace-break-spaces"
        children={
          "¡Hola a todos!\n\nCasi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.\n\nNunca lo sabrás."
        }
      />
    </div>
  }
</Example>

```html
<!-- [!code classes:whitespace-break-spaces] -->
<!-- prettier-ignore -->
<p class="whitespace-break-spaces">¡Hola a todos!

Casi es 2022       y todavía no sabemos si hay             extraterrestres viviendo entre nosotros, ¿o sí? Tal vez la persona que escribe esto sea un extraterrestre.

Nunca lo sabrás.</p>
```

</Figure>

### Diseño responsivo

<ResponsiveDesign element="p" property="white-space" defaultClass="whitespace-pre" featuredClass="whitespace-normal" />
